一.代码上传GitHub
git add .  # 添加修改的文件
git commit -m "描述本次修改"  # 提交到本地仓库
git push  # 推送到远程 GitHub 仓库（会自动使用之前关联的 origin 地址）

二.延迟代码
SwingUtilities.invokeLater(() -> {
                    Timer timer = new Timer(2000, e -> {
                        // 时间到后执行
                        currentStep++;
                        processNextStep();
                    });
                    timer.setRepeats(false);
                    timer.start();
                });

三.bug：
复活狼人后
1.复活当天与队友聊天出现异常
2.复活第二天开始倒计时显示重叠